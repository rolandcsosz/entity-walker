{
    "name": "entity-walker",
    "version": "2.2.0",
    "description": "A type-safe, in-memory entity graph resolver for normalized data structures in TypeScript.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "docs"
    ],
    "scripts": {
        "build": "npm run build:core && npm run build:api",
        "build:core": "tsc -p tsconfig.core.json",
        "build:api": "tsc -p tsconfig.api.json && cp package.api.json dist/api/package.json",
        "test": "vitest",
        "test:core": "vitest tests/core",
        "test:api": "vitest tests/api",
        "test:watch": "vitest --watch",
        "lint": "eslint .",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "benchmark": "vitest ./tests/benchmarks/benchmark.ts",
        "generate:client": "openapi-ts -i ./tests/api/openapi.json -o tests/api/generated/client -c @hey-api/client-fetch",
        "list:api": "node scripts/list-api.js",
        "api:list": "node scripts/list-api.js"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/rolandcsosz/entity-walker.git"
    },
    "keywords": [
        "entity",
        "graph",
        "typescript",
        "relations",
        "foreign-key",
        "normalized-data"
    ],
    "author": "Roland Csősz",
    "license": "MIT",
    "devDependencies": {
        "@hey-api/client-fetch": "^0.13.1",
        "@hey-api/openapi-ts": "^0.95.0",
        "@types/node": "^20.5.0",
        "eslint": "^8.50.0",
        "eslint-config-prettier": "^10.1.8",
        "prettier": "^3.9.6",
        "typescript": "^5.3.0",
        "typescript-eslint": "^8.67.0",
        "vitest": "^4.0.18"
    }
}
